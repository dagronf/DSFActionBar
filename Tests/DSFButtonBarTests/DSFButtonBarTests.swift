import XCTest
@testable import DSFButtonBar

final class DSFButtonBarTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(DSFButtonBar().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
